# Standard answers and internal state managed by Copier
# Changes here will be overwritten by Copier; NEVER EDIT MANUALLY.
# This project tracks the Beman exemplar copier template. Because the template
# currently lives on the `copier` branch of the fork (PR not yet merged),
# update with:
#     copier update --trust --vcs-ref copier
_commit: v2.4.1-35-g2313d70
_src_path: https://github.com/steve-downey/exemplar.git
description: Shape-preserving traversal and transposition for contextual computations
maintainer: steve-downey
minimum_cpp_build_version: '23'
paper: P3200
project_name: transpose
unit_test_library: catch2
# Hidden variables manually tracked because 'when: false' omits them from _copier_answers
generating_exemplar: false
owner: "steve-downey"
ci_tests_cron: "30 13 * * 3"
pre_commit_update_cron: "45 8 * * 5"
